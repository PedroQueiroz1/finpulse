spring:
  application:
    name: auth-service
  profiles:
    active: dev              # perfil padrão se nada for especificado
  config:
    import: optional:configserver:http://localhost:8888

server:
  port: 8081